import React from 'react';
import { Text, View } from 'react-native';

const TextRecognitionView = ({
    params,
}) => (
    <View>
        <Text>TextRecognitionView</Text>
    </View>
);

export default TextRecognitionView;
